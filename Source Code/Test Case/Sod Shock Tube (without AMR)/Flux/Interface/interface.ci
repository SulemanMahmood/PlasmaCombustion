module interfaceflux{
  array[4D] Intflux{
    entry void inter_flux(Msg*);
    entry void calc();
    /*entry void calc(){
      when inter_flux(Msg* m) serial{
        memcpy();
        delete m;
      }
    }*/
  };
};
