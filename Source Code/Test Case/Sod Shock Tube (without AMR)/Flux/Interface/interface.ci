module interfaceflux{
  array[4D] Intflux{
    entry void inter_flux(Msg *m);
    entry calc(){
      when inter_flux(Msg *m){
        serial {
				// Commented out because functionality is unclear for now -- Suleman
				//	memcpy();		
				//	delete m;
				}
      }
    }
  };
};
